import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.equilibrionatural.app',
  appName: 'Equilibrio Natural',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
